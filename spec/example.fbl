;; This is a comment.
;;

;; Host (Ruby) Interop:

::puts(1)
;; 1
;; => nil

::puts(1 + 2)
;; 3
;; => nil (with 3 printed to stdout)

::"omg".upcase()
;; => OMG

::"lol".upcase.length()
;; => 3

::Array.new()
;; => []

::Array
;; => %host.Array

;; -----
